#/bin/bash

# we want the line between lines starting with '+' and lines starting with '@', return a list of uniq characters from those lines. Will need to be sensitive to the last line in the file, which will have no following '@'
